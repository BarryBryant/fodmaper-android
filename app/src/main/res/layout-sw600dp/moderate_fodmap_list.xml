<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:layout_margin="@dimen/tablet_window_margin">

    <TextView
        android:layout_width="match_parent"
        android:textColor="@android:color/white"
        android:layout_height="@dimen/edit_text_height"
        android:gravity="center"
        android:textSize="@dimen/tablet_window_title_text"
        android:background="@color/colorPrimary"
        android:text="@string/moderate_fodmap_food"/>


    <android.support.v7.widget.RecyclerView
        android:layout_marginTop="@dimen/edit_text_height"
        android:id="@+id/moderate_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/moderate_advertisement_height"
        >

    </android.support.v7.widget.RecyclerView>

    <com.google.android.gms.ads.AdView
        android:id="@+id/adView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        ads:adSize="BANNER"
        ads:adUnitId="@string/banner_ad_unit_id" />


</RelativeLayout>
